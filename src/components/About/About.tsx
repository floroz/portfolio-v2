import React from "react";
import { Section } from "./About.styled";

interface Props {}

const About = (props: Props) => {
  return (
    <Section>
      <h2>Test</h2>
    </Section>
  );
};

export default About;
